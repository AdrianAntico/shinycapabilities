# Host integration compatibility

Hosts integrate through explicit capability presentation metadata, stable
`data-shinycap-*` hooks, documented CSS variables, versioned browser messages,
returned module controls, and portable workflow documents.

The package performs no domain-specific presentation inference. Host metadata
is authoritative and unknown presentation fields fail closed.

The default and installed examples are the neutral document workflow.
