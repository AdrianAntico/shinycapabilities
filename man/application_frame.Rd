% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/application_frame.R
\name{application_frame}
\alias{application_frame}
\title{Compose named application regions without owning application state}
\usage{
application_frame(
  id,
  work,
  header = NULL,
  navigation = NULL,
  context = NULL,
  inspector = NULL,
  parameters = NULL,
  output = NULL,
  actions = NULL,
  status = NULL,
  theme = c("auto", "light", "dark"),
  density = c("compact", "comfortable", "dense"),
  tokens = NULL,
  commands = list()
)
}
\arguments{
\item{id}{Stable frame identifier, namespaced by the caller when appropriate.}

\item{work}{Main work area, containing ordinary Shiny tags or outputs.}

\item{header, navigation, context, inspector, parameters, output, actions, status}{Optional regions.}

\item{theme}{Presentation theme: auto inherits Bootstrap variables; light/dark are explicit.}

\item{density}{Presentation spacing only: comfortable, compact, or dense.}

\item{tokens}{Named CSS custom-property overrides, such as `c("--sc-primary" = "#146b52")`.}

\item{commands}{List of [command_record()] values. Invocation is not authorization.}
}
\value{
A Shiny tag with named regions, token scope, and a shared overlay root.
}
\description{
Compose named application regions without owning application state
}
\details{
The frame is not a router, state store, scheduler, or session manager.
  All commands remain inspectable via `ShinyCapabilitiesFoundation.commands(id)`.
  Invocations publish `input[[paste0(id, "_command")]]`; hosts must authorize them.
}
\examples{
application_frame("app", work = browser_text_field("name", "Name"),
  status = "Ready", density = "compact")
}
